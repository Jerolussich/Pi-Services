services:
  itau-email-tracker:
    build:
      context: ./tracker
      dockerfile: Dockerfile
    container_name: itau-email-tracker
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    labels:
      ofelia.enabled: "true"
      ofelia.job-exec.itau-email-tracker.schedule: "0 * * * *"
      ofelia.job-exec.itau-email-tracker.command: "python /app/fetch.py"
  finance-tracker-ui:
    build:
      context: ./ui
      dockerfile: Dockerfile
    container_name: finance-tracker-ui
    volumes:
      - ./data:/app/data
      - ./tracker/fetch.py:/app/fetch.py:ro
    env_file:
      - .env
    restart: unless-stopped
networks:
  default:
    name: pi-services
    external: true
